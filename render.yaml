# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-todo-app-2-46
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
